# Dotfiles

## Content
* zshrc
* vimrc (with plugins)
* lesskey
* gitconfig (needs to be configured)

## Installation
	git clone https://github.com/fdw/dotfiles.git ~/.config
	cd .config/dotfiles
	make install

## Updating
	make update
